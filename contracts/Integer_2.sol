pragma solidity ^0.4.24;

/* 
 * Level 2: 
 * Edit this file to pass all the tests in Test_Nil_2.sol
 */

contract Integer_2 {

    // TODO: fix this function so it returns an uninitiated uint
    function returnNil() view returns(uint) {
        return 5;
    }


// How does solidity handle undefined or nil values 

// comparitors: Comparisons: <=, <, ==, !=, >=, > (evaluate to bool)

// Bit operators: &, |, ^ (bitwise exclusive or), ~ (bitwise negation)


// Arithmetic operators: +, -, unary -, unary +, *, /, % (remainder), ** (exponentiation), << (left shift), >> (right shift)


}